# Copy this file to .env and fill in real values. Never commit .env itself.

# --- Local Postgres (used by docker-compose.yml and the backend) ---
POSTGRES_USER=repeat_signal
POSTGRES_PASSWORD=repeat_signal_dev
POSTGRES_DB=repeat_signal
DATABASE_URL=postgresql://repeat_signal:repeat_signal_dev@localhost:5432/repeat_signal

# --- Backend (FastAPI) ---
# Populated in Milestone 3.

# --- Frontend (Next.js) ---
# Populated in Milestone 4. Will include e.g. NEXT_PUBLIC_API_BASE_URL.
