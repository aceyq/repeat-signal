# data/processed/

Cleaned, normalized, and pre-aggregated datasets produced by `../../scripts/` from `../raw/`. This is what the backend loads into PostgreSQL. Large files are gitignored; small reference outputs may be committed for reproducibility.

Populated starting in Milestone 2. See [../../docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md) for the target schema.
