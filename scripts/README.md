# scripts/

Standalone, repeatable Python scripts that productionize logic first prototyped in `../notebooks/`. Unlike notebooks, these are meant to be run end-to-end (e.g. `python scripts/build_processed_dataset.py`) as part of the data pipeline, and are safe to schedule or re-run.

Populated starting in Milestone 2.
